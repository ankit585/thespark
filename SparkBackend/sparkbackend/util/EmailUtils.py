import pprint
from sparkbackend.util.Callable import Callable

class EmailsUtils():

    def sendRegistrationMail(userKey,code):
        """ send registration mail
        """

    sendRegistrationMail = Callable(sendRegistrationMail)

